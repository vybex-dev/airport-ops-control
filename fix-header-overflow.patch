diff --git a/src/components/layout/TopStatusBar.tsx b/src/components/layout/TopStatusBar.tsx
index cf96b10..34a40cc 100644
--- a/src/components/layout/TopStatusBar.tsx
+++ b/src/components/layout/TopStatusBar.tsx
@@ -22,32 +22,32 @@ export const TopStatusBar: React.FC<TopStatusBarProps> = ({
 
   return (
     <header className="sticky top-0 z-30 w-full border-b border-line bg-surface-0/95 backdrop-blur-md">
-      <div className="flex items-center justify-between gap-sm px-md py-xs">
+      <div className="flex items-center justify-between gap-sm px-md py-xs min-w-0">
         {/* Left Section: Mobile Nav Toggle + Airport Identity */}
-        <div className="flex items-center gap-md">
+        <div className="flex items-center gap-md min-w-0">
           {onToggleMobileNav && (
             <button
               type="button"
               onClick={onToggleMobileNav}
               aria-label={isMobileNavOpen ? 'Close Navigation Menu' : 'Open Navigation Menu'}
               aria-expanded={isMobileNavOpen}
-              className="lg:hidden p-1.5 rounded text-ink-muted hover:text-ink-primary hover:bg-surface-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-signal"
+              className="lg:hidden p-1.5 rounded text-ink-muted hover:text-ink-primary hover:bg-surface-2 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-signal shrink-0"
             >
               {isMobileNavOpen ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}
             </button>
           )}
 
-          <div className="flex items-center gap-xs">
-            <div className="flex items-center justify-center h-8 w-10 rounded bg-accent-signal/15 border border-accent-signal/40 text-accent-signal font-data text-xs font-bold tracking-widest select-none shadow-[0_0_10px_rgba(0,229,255,0.15)]">
+          <div className="flex items-center gap-xs min-w-0">
+            <div className="flex items-center justify-center h-8 w-10 rounded bg-accent-signal/15 border border-accent-signal/40 text-accent-signal font-data text-xs font-bold tracking-widest select-none shadow-[0_0_10px_rgba(0,229,255,0.15)] shrink-0">
               DEL
             </div>
 
-            <div>
-              <div className="flex items-center gap-2xs">
-                <h1 className="font-display text-xs sm:text-sm font-semibold tracking-wide text-ink-primary truncate max-w-[160px] sm:max-w-none">
+            <div className="min-w-0">
+              <div className="flex items-center gap-2xs min-w-0">
+                <h1 className="font-display text-xs sm:text-sm font-semibold tracking-wide text-ink-primary truncate min-w-0 max-w-[110px] xs:max-w-[160px] sm:max-w-none">
                   Indira Gandhi International Airport
                 </h1>
-                <span className="font-data text-[9px] sm:text-[10px] text-ink-muted px-1.5 py-0.5 rounded bg-surface-2 border border-line shrink-0">
+                <span className="font-data text-[9px] sm:text-[10px] text-ink-muted px-1.5 py-0.5 rounded bg-surface-2 border border-line shrink-0 hidden xs:inline-block">
                   VIDP / T3 NOC
                 </span>
               </div>
@@ -81,7 +81,7 @@ export const TopStatusBar: React.FC<TopStatusBarProps> = ({
         </div>
 
         {/* Right Section: KPI Tiles, Global Alert Trigger & Search */}
-        <div className="flex items-center gap-sm">
+        <div className="flex items-center gap-xs sm:gap-sm shrink-0">
           <div className="hidden xl:flex items-center gap-xs">
             {/* Active Flights Tile */}
             <div className="flex items-center gap-2xs px-xs py-1 rounded bg-surface-1 border border-line">
@@ -106,7 +106,7 @@ export const TopStatusBar: React.FC<TopStatusBarProps> = ({
           <button
             type="button"
             onClick={openAlertsDrawer}
-            className={`flex items-center gap-2xs px-xs py-1 rounded border transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-signal ${
+            className={`flex items-center gap-2xs px-xs py-1 rounded border transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-signal shrink-0 ${
               unacknowledgedCount > 0
                 ? 'bg-status-alert/15 border-status-alert/50 text-status-alert shadow-[0_0_10px_rgba(244,63,94,0.2)] hover:bg-status-alert/25'
                 : 'bg-surface-1 border-line text-ink-muted hover:text-ink-primary'
@@ -124,7 +124,7 @@ export const TopStatusBar: React.FC<TopStatusBarProps> = ({
           <SearchInput
             value={searchQuery}
             onChange={setSearchQuery}
-            className="w-36 focus-within:w-48 sm:w-48 sm:focus-within:w-60 md:w-60 md:focus-within:w-72 lg:w-72 lg:focus-within:w-80 shrink-0"
+            className="w-20 focus-within:w-40 xs:w-28 xs:focus-within:w-44 sm:w-48 sm:focus-within:w-60 md:w-60 md:focus-within:w-72 lg:w-72 lg:focus-within:w-80 shrink min-w-0"
           />
         </div>
       </div>
diff --git a/src/index.css b/src/index.css
index 479b784..96719ee 100644
--- a/src/index.css
+++ b/src/index.css
@@ -21,6 +21,12 @@
  * ============================================================
  */
 @theme {
+  /* Extra breakpoint below Tailwind's default 'sm' (640px) — needed
+     because the header must shed elements gradually starting well
+     below 640px on real phones (e.g. 360-390px viewports), not jump
+     straight from "mobile" to "sm" in one step. */
+  --breakpoint-xs: 22.5rem; /* 360px */
+
   /* --- Surfaces: deep blue-black, not neutral/true black --- */
   --color-surface-0: #0a0e14; /* app background */
   --color-surface-1: #111826; /* panel/card background */
